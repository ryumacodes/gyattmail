# Encryption Key (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=your_32_byte_hex_key_here

# Gmail OAuth (from Google Cloud Console)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/gmail/callback

# Outlook OAuth (from Azure Portal)
OUTLOOK_CLIENT_ID=your_outlook_client_id
OUTLOOK_CLIENT_SECRET=your_outlook_client_secret
OUTLOOK_REDIRECT_URI=http://localhost:3000/api/auth/outlook/callback

# iCloud (Manual IMAP/SMTP - requires app-specific password)
# No OAuth needed, configured per-account

# Optional: AI Features (OpenAI or Gemini)
# OPENAI_API_KEY=your_openai_key
# GEMINI_API_KEY=your_gemini_key

# Optional: Database URL (if using external DB instead of localStorage)
# DATABASE_URL=postgresql://...

# Optional: Redis for caching
# REDIS_URL=redis://...

# Next.js
NEXT_PUBLIC_APP_URL=http://localhost:3000
